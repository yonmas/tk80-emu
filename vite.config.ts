import { defineConfig } from "vite";

// Served from https://<user>.github.io/tk80-emu/ via GitHub Pages,
// so asset URLs need the repo name as a base path.
export default defineConfig({
  base: "/tk80-emu/",
});
